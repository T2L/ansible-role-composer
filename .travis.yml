---

sudo: required
dist: trusty
services:
  - docker

language: ruby
rvm:
  - 2.2.3

before_install:
  - gem update --system
  - gem --version

install:
  - gem install test-kitchen kitchen-docker kitchen-ansible

script:
  - kitchen test
